# Ko/En 동기화 누락 리포트 — design-architect

> 작성일: 2026-04-09
> 비교 대상: `src/pages/ko/index.astro` (476줄, 인라인 HTML) vs `src/pages/en/index.astro` (68줄, React 컴포넌트)

---

## 요약 / Summary

| 구분 | ko/index.astro | en/index.astro | 동기화 상태 |
|------|---------------|---------------|------------|
| 아키텍처 | 인라인 HTML + CSS 애니메이션 + `<script>` | React 컴포넌트 import | **완전 분리** |
| 총 줄 수 | 476 | 68 | — |
| 모션 시스템 | CSS `.animate-fade-up` + IntersectionObserver script | React ScrollReveal + useReducedMotion | **불일치** |
| 접근성(reduced-motion) | 미지원 | useReducedMotion 훅 적용 | **ko 누락** |

---

## 1. Hero 섹션

| 항목 | ko | en (Hero.tsx) | Gap |
|------|-----|--------------|-----|
| SplitText (I2) | 없음 — CSS `.animate-fade-up` | SplitText 컴포넌트 | **누락** |
| CursorSpotlight (I5) | 없음 | CursorSpotlight 컴포넌트 | **누락** |
| MagneticButton (I6) | 없음 | MagneticButton 컴포넌트 | **누락** |
| CountUp (I3) | 있음 (lines 46-56, `<script>`) | CountUp 컴포넌트 | 기능 동일, 구현 상이 |
| reduced-motion 대응 | 없음 | useReducedMotion 훅 | **누락** |

### 세부 사항
- ko의 CountUp은 `<script>` 태그 내 IntersectionObserver로 구현. en은 React 컴포넌트.
- ko의 fade-up 애니메이션은 CSS `@keyframes fadeUp`으로 처리, `prefers-reduced-motion` 미대응.

---

## 2. About 섹션

| 항목 | ko | en (About.tsx) | Gap |
|------|-----|---------------|-----|
| 그리드 비율 | 5+7 (`lg:col-span-5` + `lg:col-span-7`) | 4+8 리디자인 (`lg:grid-cols-[280px_1fr]`) | **불일치** |
| 블록쿼트 | 없음 | `border-l-[3px]` accent blockquote | **누락** |
| ScrollReveal (I1) | 없음 | 5개 ScrollReveal 래퍼 (stagger 0-400ms) | **누락** |
| 스킬 태그 | 하드코딩 한국어만 | 이중 언어 `skills[lang]` | 기능 동일, 확장성 상이 |
| 프로필 이미지 | 있음 | 있음 | 동일 |

### 세부 사항
- ko는 P3.2 리디자인(비대칭 그리드, 블록쿼트, ScrollReveal)이 전혀 반영되지 않음.
- 스킬 태그 목록 자체도 ko/en 간 다를 수 있음 (ko는 하드코딩).

---

## 3. Projects 섹션

| 항목 | ko | en (Projects.tsx) | Gap |
|------|-----|------------------|-----|
| TiltCard (I4) | 없음 — 단순 hover scale | TiltCard 컴포넌트 | **누락** |
| ColorPlaceholder | 있음 (인라인) | ColorPlaceholder 컴포넌트 | 구현 상이 |
| 카드 레이아웃 | 인라인 grid (lines 182-210) | React 컴포넌트 grid | 유사하나 스타일 차이 가능 |
| 프로젝트 데이터 소스 | 하드코딩 HTML | `translations.ts` or props | **불일치** |

---

## 4. Journey 섹션

| 항목 | ko | en (Journey.tsx) | Gap |
|------|-----|-----------------|-----|
| 레이아웃 | 수직 타임라인 (커스텀 CSS) | P3.6 수직 센터라인 타임라인 | **구조 유사, 구현 완전 상이** |
| 좌우 교대 | 없음 (좌측 정렬) | lg에서 좌/우 교대 (`isLeft = i % 2 === 0`) | **누락** |
| ScrollReveal (I1) | 없음 — CSS `.timeline-item.visible` | ScrollReveal 래퍼 (stagger 100ms) | **누락** |
| 데이터 항목 | 4개 커스텀 (금융 커리어 전환 준비 포함, 아이콘 포함) | `translations.ts`의 `timeline.items` 4개 | **내용 불일치** |
| Accent dot 스타일 | 커스텀 원형 아이콘 | 첫째 filled, 나머지 hollow | **불일치** |
| 스크롤 라인 프로그레스 | 커스텀 CSS 애니메이션 | React state 기반 | **구현 상이** |

### 세부 사항
- ko의 타임라인 항목에는 고유 아이콘(briefcase, code 등)이 있으나 en에는 없음.
- ko의 항목 내용이 `translations.ts`와 다를 수 있음 (하드코딩이므로).

---

## 5. ko에만 존재하는 섹션

| 섹션 | ko 줄 번호 | en 대응 | 비고 |
|------|-----------|---------|------|
| **Methodology (sticky)** | 93-137 | 없음 | "접근 방식" 스티키 스크롤 섹션 |
| **Capabilities (radar SVG)** | 139-178 | 없음 | SVG 레이더 차트 |
| **Full-bleed CTA** | 326-350 | 없음 | 별도 CTA 섹션 |

### 판단 필요
- 이 섹션들이 의도적 ko-only인지, en에도 추가해야 하는지 pm-orchestrator 결정 필요.

---

## 6. en에만 존재하는 컴포넌트

| 컴포넌트 | en import | ko 대응 | 비고 |
|----------|-----------|---------|------|
| **Skills** | `<Skills>` | 없음 | 스킬 섹션 자체 부재 |
| **Certifications** | `<Certifications>` | 없음 | 자격증 섹션 부재 |
| **Contact** | `<Contact>` | 없음 | 연락처 섹션 부재 |
| **BottomNav** | `<BottomNav>` | 없음 | 하단 내비게이션 부재 |

---

## 7. 누락 인터랙션 매트릭스

| ID | 인터랙션 | en 적용 | ko 적용 | 우선순위 |
|----|---------|---------|---------|---------|
| I1 | ScrollReveal | About, Journey, Projects | **없음** | **P0** |
| I2 | SplitText | Hero headline | **없음** | **P0** |
| I3 | CountUp | Hero stats | Hero stats (script) | P2 (기능 동일) |
| I4 | TiltCard | Projects | **없음** | **P1** |
| I5 | CursorSpotlight | Hero | **없음** | P1 |
| I6 | MagneticButton | Hero CTA | **없음** | P1 |
| I7 | StickyTOC progress | Blog detail | Blog detail | 동일 (별도 페이지) |
| I8 | PageTransition | 전역 | 전역 | 동일 (Layout 레벨) |

---

## 8. 권장 동기화 전략

### Option A: ko를 React 컴포넌트로 전환 (권장)
- ko/index.astro를 en/index.astro와 동일 구조로 변경
- 각 컴포넌트가 `lang` prop으로 한/영 분기
- **장점**: 단일 소스, 유지보수 용이, 인터랙션 자동 동기화
- **비용**: ko 전용 섹션(Methodology, Capabilities, CTA) 컴포넌트화 필요

### Option B: ko 인라인 HTML에 모션 컴포넌트 island 삽입
- 기존 인라인 구조 유지, Astro island로 모션만 주입
- **장점**: 변경 범위 최소
- **단점**: 이중 관리 지속, 향후 동기화 부담 누적

### 권장: **Option A** — PRD가 ko/en 동일 UX를 전제하므로, 컴포넌트 공유가 필수.

---

## 9. 접근성 경고

ko/index.astro의 모든 CSS 애니메이션(`.animate-fade-up`, `.timeline-item.visible`)은 `prefers-reduced-motion` 미대응.
PRD §7 AC(접근성 준수)를 위해 ko 전환 시 반드시 `useReducedMotion` 훅 또는 CSS `@media (prefers-reduced-motion: reduce)` 적용 필요.

---

*Generated by design-architect agent*
