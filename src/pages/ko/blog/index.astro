---
import Layout from '../../../layouts/Layout.astro';
import Navbar from '../../../components/Navbar';
import Footer from '../../../components/Footer';
import { getCollection } from 'astro:content';
import { t } from '../../../i18n/translations';

const lang = 'ko';
const tr = t(lang);

const posts = (await getCollection('blog')).sort(
  (a, b) => b.data.date.valueOf() - a.data.date.valueOf()
);

const categoryLabels: Record<string, string> = {
  project: '프로젝트',
  planning: '기획',
  'deep-dive': '삽질기',
  skill: '스킬',
};

const base = import.meta.env.BASE_URL;
---

<Layout title={`${tr.blog.pageTitle} — HYEBIN.DEV`}>
  <Navbar lang={lang} client:load />

  <main class="px-6 pt-32 pb-20">
    <div class="mx-auto max-w-7xl">
      <p class="mb-8 text-xs uppercase tracking-[0.15em] text-[var(--color-text-faint)]">
        {tr.blog.section}
      </p>
      <h1 class="mb-4 text-4xl font-bold sm:text-5xl text-[var(--color-text)]" style="font-family: var(--font-display);">
        {tr.blog.pageTitle}
      </h1>
      <p class="mb-10 max-w-xl text-sm leading-relaxed text-[var(--color-text-muted)]">
        {tr.blog.pageDescription}
      </p>

      <div class="mb-12 flex flex-wrap gap-2" id="category-filters">
        <button
          data-category="all"
          class="category-btn active rounded-full bg-[var(--color-accent)] px-4 py-1.5 font-mono text-[11px] uppercase tracking-[0.15em] text-[var(--color-on-accent)] transition-all"
        >
          전체
        </button>
        {Object.entries(categoryLabels).map(([key, label]) => (
          <button
            data-category={key}
            class="category-btn rounded-full bg-[var(--color-bg-card)] px-4 py-1.5 font-mono text-[11px] uppercase tracking-[0.15em] text-[var(--color-text-muted)] shadow-ambient-sm transition-all hover:bg-[var(--color-secondary-container)] hover:text-[var(--color-on-secondary-container)]"
          >
            {label}
          </button>
        ))}
      </div>

      <div class="space-y-0">
        {posts.map((post, i) => (
          <a
            href={`${base}${lang}/blog/${post.id}/`}
            data-category={post.data.category}
            class={`blog-post group grid items-start gap-6 py-10 lg:grid-cols-[120px_1fr_1fr] ${
              i !== posts.length - 1 ? 'border-b border-[var(--color-border)]' : ''
            }`}
          >
            <div class="flex items-center gap-3">
              <span class="text-xs uppercase tracking-[0.15em] text-[var(--color-accent)]">
                {categoryLabels[post.data.category] || post.data.category}
              </span>
            </div>
            <div>
              <h2 class="mb-2 text-xl font-bold text-[var(--color-text)] transition-colors group-hover:text-[var(--color-accent)]" style="font-family: var(--font-display);">
                {post.data.title}
              </h2>
              <div class="flex flex-wrap gap-2">
                {post.data.tags.map((tag: string) => (
                  <span class="rounded-full bg-[var(--color-secondary-container)] px-2.5 py-0.5 text-[10px] font-medium text-[var(--color-on-secondary-container)]">
                    {tag}
                  </span>
                ))}
              </div>
            </div>
            <div>
              <p class="mb-3 text-sm leading-[1.8] text-[var(--color-text-muted)]">
                {post.data.description}
              </p>
              <p class="font-mono text-[10px] text-[var(--color-text-faint)]">
                {post.data.date.toISOString().slice(0, 10)}
              </p>
            </div>
          </a>
        ))}
      </div>
    </div>
  </main>

  <Footer />
</Layout>

<script>
  const buttons = document.querySelectorAll<HTMLButtonElement>('.category-btn');
  const posts = document.querySelectorAll<HTMLElement>('.blog-post');

  buttons.forEach(btn => {
    btn.addEventListener('click', () => {
      const category = btn.dataset.category;

      buttons.forEach(b => {
        b.classList.remove('active', 'bg-[var(--color-accent)]', 'text-[var(--color-on-accent)]');
        b.classList.add('bg-[var(--color-bg-card)]', 'text-[var(--color-text-muted)]');
      });
      btn.classList.remove('bg-[var(--color-bg-card)]', 'text-[var(--color-text-muted)]');
      btn.classList.add('active', 'bg-[var(--color-accent)]', 'text-[var(--color-on-accent)]');

      posts.forEach(post => {
        if (category === 'all' || post.dataset.category === category) {
          post.style.display = '';
        } else {
          post.style.display = 'none';
        }
      });
    });
  });
</script>
